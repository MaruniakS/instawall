<nav role="navigation" class="navbar navbar-default">
  <div class="navbar-header">
    <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <em><%= link_to 'MySite', root_path, {class: 'navbar-brand'}  %>
    </em>
  </div>
  <div id="navbarCollapse" class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
      <li class="active"><a href="#">Home</a></li>
      <li><a href="#">Profile</a></li>
      <li><a href="#">Messages</a></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if user_signed_in? %>
          <li> <p class='nav navbar-text'>Logged as <strong><%= Identity.find_by_user_id(current_user.id).nickname %></strong>.</p></li>
          <li>
            <%= image_tag  Identity.find_by_user_id(current_user.id).image, :class => 'img-circle', :size => '50x50'%>
          </li>
          <li>
            <%= link_to destroy_user_session_path, :method => :delete do %>
                <span class="glyphicon glyphicon-off"></span> Logout
            <% end %>
          </li>
      <% else %>
          <li>
            <%= link_to  omniauth_authorize_path(resource_name, 'instagram'), {class: 'btn-default'} do %>
                <span class="glyphicon glyphicon-log-in"></span> Login with Instagram
            <% end %>
          </li>
      <% end %>
    </ul>
  </div>
</nav>